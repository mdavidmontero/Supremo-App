export interface Vehicle {
  cedula: string;
  nombreCompleto: string;
  model: string;
  placa: string;
  vin: string;
  modelo: string;
}

export interface manualInput {
  cedula: string;
  nombreCompleto: string;
  placa: string;
  vin: string;
  modelo: string;
}

export interface decodedScanner {
  cedula: string;
  nombreCompleto: string;
  placa: string;
  vin: string;
  modelo: string;
}
